[test]

# to set different thresholds for lines and functions
coverageThreshold = { lines = 0.9, functions = 0.9, statements = 0.9 }

[install.scopes]
"@durkinza" = { url = "https://npm.pkg.github.com/", token = "$GITHUB_TOKEN" }